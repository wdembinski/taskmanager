/**
 * The one colour that means "this wants you".
 *
 * It was written out three times — the card's ring, the status bar's attention state,
 * and the agent panel — and the GitLab work would have made it four. Three copies of a
 * hex value is a colour that drifts; this is the single definition.
 *
 * Not a Fluent token because it is deliberately outside the theme: the point is that it
 * is the only warm colour on a cool board, and a themeable one would stop being that.
 */
export const UNREAD_ORANGE = '#F2A900';
