{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "target": "ES2022",
    "lib": ["ES2022"],
    "outDir": "dist",
    "rootDir": "src",
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "resolveJsonModule": true,
    "noUnusedLocals": false,
    "noUnusedParameters": false
  },
  "//": "The TYPECHECK config (`tsc --noEmit -p tsconfig.json`), which is why *.test.ts is NOT excluded here: the tests are source and get checked like the rest of it. Emitting is tsconfig.build.json's job, and that is where the test exclusion lives so dist/ stays free of them.",
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
